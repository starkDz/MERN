import { createTheme } from "@mui/material/styles";

export const globaleTheme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#232369",
    },
    secondary: {
      main: "#f50057",
    },
  },
});
